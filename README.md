# Webpack-tailwind-sinalePageApp-Starter

A simple starting point with **tailwindcss**.

To run the development server:

`npm run dev`

To build the project

`npm run build`

When built for production the css and js and html will be minified.

---
